/*
Deployment script for TripleG_SQLDB

This code was generated by a tool.
Changes to this file may cause incorrect behavior and will be lost if
the code is regenerated.
*/

GO
SET ANSI_NULLS, ANSI_PADDING, ANSI_WARNINGS, ARITHABORT, CONCAT_NULL_YIELDS_NULL, QUOTED_IDENTIFIER ON;

SET NUMERIC_ROUNDABORT OFF;


GO
:setvar DatabaseName "TripleG_SQLDB"
:setvar DefaultFilePrefix "TripleG_SQLDB"
:setvar DefaultDataPath "C:\Users\William\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"
:setvar DefaultLogPath "C:\Users\William\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB"

GO
:on error exit
GO
/*
Detect SQLCMD mode and disable script execution if SQLCMD mode is not supported.
To re-enable the script after enabling SQLCMD mode, execute the following:
SET NOEXEC OFF; 
*/
:setvar __IsSqlCmdEnabled "True"
GO
IF N'$(__IsSqlCmdEnabled)' NOT LIKE N'True'
    BEGIN
        PRINT N'SQLCMD mode must be enabled to successfully execute this script.';
        SET NOEXEC ON;
    END


GO
USE [$(DatabaseName)];


GO
/*
The type for column Password in table [dbo].[Account] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[Account])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The type for column CardHolderName in table [dbo].[CreditCard] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.

The type for column CreditCardNumber in table [dbo].[CreditCard] is currently  NCHAR (10) NULL but is being changed to  INT NULL. Data loss could occur.

The type for column SecurityNumber in table [dbo].[CreditCard] is currently  NCHAR (10) NULL but is being changed to  INT NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[CreditCard])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The type for column EmailAddress in table [dbo].[Customer] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.

The type for column Name in table [dbo].[Customer] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.

The type for column PhoneNumber in table [dbo].[Customer] is currently  NCHAR (10) NULL but is being changed to  INT NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[Customer])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The type for column DepartmentName in table [dbo].[Department] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[Department])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
/*
The type for column Name in table [dbo].[Employee] is currently  NCHAR (10) NULL but is being changed to  VARCHAR (50) NULL. Data loss could occur.
*/

IF EXISTS (select top 1 1 from [dbo].[Employee])
    RAISERROR (N'Rows were detected. The schema update is terminating because data loss might occur.', 16, 127) WITH NOWAIT

GO
PRINT N'Altering [dbo].[Account]...';


GO
ALTER TABLE [dbo].[Account] ALTER COLUMN [Password] VARCHAR (50) NULL;


GO
PRINT N'Altering [dbo].[CreditCard]...';


GO
ALTER TABLE [dbo].[CreditCard] ALTER COLUMN [CardHolderName] VARCHAR (50) NULL;

ALTER TABLE [dbo].[CreditCard] ALTER COLUMN [CreditCardNumber] INT NULL;

ALTER TABLE [dbo].[CreditCard] ALTER COLUMN [ExpiryDate] DATE NULL;

ALTER TABLE [dbo].[CreditCard] ALTER COLUMN [SecurityNumber] INT NULL;


GO
PRINT N'Altering [dbo].[Customer]...';


GO
ALTER TABLE [dbo].[Customer] ALTER COLUMN [EmailAddress] VARCHAR (50) NULL;

ALTER TABLE [dbo].[Customer] ALTER COLUMN [Name] VARCHAR (50) NULL;

ALTER TABLE [dbo].[Customer] ALTER COLUMN [PhoneNumber] INT NULL;


GO
PRINT N'Altering [dbo].[Department]...';


GO
ALTER TABLE [dbo].[Department] ALTER COLUMN [DepartmentName] VARCHAR (50) NULL;


GO
PRINT N'Altering [dbo].[Employee]...';


GO
ALTER TABLE [dbo].[Employee] ALTER COLUMN [Name] VARCHAR (50) NULL;


GO
PRINT N'Update complete.';


GO
